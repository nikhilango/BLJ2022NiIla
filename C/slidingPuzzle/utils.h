#ifndef UTILS_H
#define UTILS_H
void printField(int size, int field[size][size]);
void swapValues(int *num1, int *num2);
int slideLeft(int x, int y);
int slideRight(int x, int y);
int slideUp(int x, int y);
int slideDown(int x, int y);
#endif

